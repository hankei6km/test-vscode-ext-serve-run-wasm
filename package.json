{
  "name": "test-vscode-ext-serve-run-wasm",
  "version": "0.1.3",
  "displayName": "Experimental - Run wasm",
  "description": "Run wasm on VS Code Web Shell",
  "keywords": [
    "wasm",
    "wasi",
    "webshell"
  ],
  "author": "hankei6km <hankei6km@gmail.com> (https://github.com/hankei6km)",
  "publisher": "hankei6km",
  "repository": {
    "type": "git",
    "url": "https://github.com/hankei6km/test-vscode-ext-serve-run-wasm"
  },
  "bugs": {
    "url": "https://github.com/hankei6km/test-vscode-ext-serve-run-wasm/issues"
  },
  "engines": {
    "vscode": "^1.82.0"
  },
  "categories": [
    "Other"
  ],
  "license": "MIT",
  "main": "./out/extension.js",
  "activationEvents": [],
  "contributes": {
    "webShellMountPoints": [
      {
        "mountPoint": "/usr/bin/rw",
        "command": "run-wasm.webshell-rw"
      }
    ]
  },
  "scripts": {
    "build": "npm run compile",
    "vscode:prepublish": "npm run clean && npm run build && bash scripts/gen-changelog.sh",
    "build:rust": "cargo build --target wasm32-wasi --release && cp target/wasm32-wasi/release/rust-hello.wasm wasm/bin/hello.wasm",
    "compile": "node esbuild.config.mjs",
    "test": "npm run test:unit && npm run test:electron",
    "pretest": "rimraf tmp && mkdir tmp && cp wasm/bin/hello.wasm tmp/hello.wasm",
    "test:unit": "mocha --require ts-node/register --require source-map-support/register --watch-extensions ts --ui tdd src/extension/test/unit/**/*.test.ts",
    "test:electron": "xvfb-run node ./out/test/runTest.js",
    "pretest:electron": "node esbuild.test-electron.config.mjs",
    "lint:type-check": "tsc --noEmit",
    "clean": "rimraf out/"
  },
  "extensionDependencies": [
    "ms-vscode.wasm-wasi-core",
    "ms-vscode.webshell"
  ],
  "dependencies": {
    "@vscode/wasm-wasi": "^0.13.0-pre.1",
    "minimist": "^1.2.8"
  },
  "devDependencies": {
    "@esbuild-plugins/node-modules-polyfill": "^0.2.2",
    "@types/minimist": "^1.2.2",
    "@types/mocha": "^10.0.1",
    "@types/node": "^20.6.5",
    "@types/vscode": "^1.82.0",
    "@vscode/test-electron": "^2.3.4",
    "@vscode/vsce": "^2.21.0",
    "assert": "^2.1.0",
    "esast-util-from-js": "^2.0.1",
    "esbuild": "^0.19.3",
    "estree-util-to-js": "^2.0.0",
    "glob": "^10.3.7",
    "mocha": "^10.2.0",
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
